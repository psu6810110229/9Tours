# Dependencies
node_modules/

# Production / Build
dist/
build/

# Environment Variables (ความลับ ห้ามเอาขึ้น Git เด็ดขาด)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Docker
.dockerignore
# (หมายเหตุ: บางทีมเอา .dockerignore ขึ้นปกติ แต่กันไฟล์ที่ docker สร้างไว้)

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Showcase DB dump (contains user rows, cloud-transform-only)
showcase_dump.sql
showcase_transform.sql

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/documentation/TEST_PLANNING.md